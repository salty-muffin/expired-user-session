name: spirit-box
channels:
  - pytorch
  - nvidia
dependencies:
  - python=3.10
  - pip=21
  - conda-forge::ffmpeg<7
  - pytorch
  - torchvision
  - torchaudio
  - pytorch-cuda=11.8
  - pip:
      - python-dotenv==1.0.1
      - click==8.1.7
      - nltk==3.9.1
      # bark & text generation
      - huggingface-hub
      - transformers
      - encodec
      # whisper
      - openai-whisper==20231117
      - pydub==0.25.1
      # socketio
      - python-socketio==5.11.4
      - eventlet==0.36.1
